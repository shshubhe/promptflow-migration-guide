# Root requirements — covers all samples in this repo.
# MAF 1.0 is GA (released 2 Apr 2026). No --pre flag needed for core packages.
# Preview connectors (e.g. agent-framework-copilotstudio) still require --pre.

agent-framework>=1.0.0
agent-framework-azure-ai-search>=1.0.0
agent-framework-foundry>=1.0.0

# agent-framework-azure-ai was removed in MAF 1.0; its functionality moved to
# agent-framework-foundry (used by the Foundry project endpoint sample in
# phase-4-migrate-ops/4b-deployment/foundry_client.py).

# Evaluation (Phase 3)
azure-ai-evaluation

# Deployment wrapper (Phase 4b)
fastapi
uvicorn
python-dotenv

# Tracing (Phase 4a)
azure-monitor-opentelemetry

# Azure identity — used by Foundry client alternative
azure-identity

# Parity check (Phase 3)
pandas
